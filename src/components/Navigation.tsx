'use client'

import { useState } from 'react'

interface Category {
  id: string
  name: string
  slug: string
}

export function Navigation({ categories }: { categories: Category[] }) {
  return null
}
